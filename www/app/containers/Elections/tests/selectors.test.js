// import { fromJS } from 'immutable';
// import { makeSelectElectionsDomain } from '../selectors';

// const selector = makeSelectElectionsDomain();

describe('makeSelectElectionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
