// import { fromJS } from 'immutable';
// import { makeSelectElectionDomain } from '../selectors';

// const selector = makeSelectElectionDomain();

describe('makeSelectElectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
